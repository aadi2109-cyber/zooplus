# Coupons validation

Tested the known coupons endpoint and requested trailing-slash, ID, and query variations unauthenticated. Authenticated coupon enumeration was not attempted because no authorized session was available.
