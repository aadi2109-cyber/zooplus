# Bonus-points validation

Tested the known balance endpoint unauthenticated, documented path/version variations, and preserved complete `curl -sv` output. Authenticated Account A/B tests were blocked because no authorized account sessions were available in the workspace.
